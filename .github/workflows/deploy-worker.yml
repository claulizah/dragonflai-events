name: Deploy Worker

# Se dispara solo cuando cambian archivos relacionados con el Worker —
# subir un cambio al frontend (dragonflai-v2.html, index.html, etc.) NO
# vuelve a desplegar el Worker innecesariamente. workflow_dispatch permite
# correrlo a mano desde la pestaña Actions, para probar sin tocar código.
on:
  push:
    branches:
      - Milestone
      - main
    paths:
      - 'worker.js'
      - 'package.json'
      - 'wrangler.toml'
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v6
      - name: Deploy Worker
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
